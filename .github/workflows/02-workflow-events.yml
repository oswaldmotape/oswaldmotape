name: 02-workfloe-events

on: push

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: Show the triggering event
              run: echo "I have been triggered by the ${{ github.event_name }} event"